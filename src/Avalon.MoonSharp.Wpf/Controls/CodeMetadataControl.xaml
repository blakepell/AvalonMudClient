<UserControl x:Class="MoonSharp.Interpreter.Wpf.Controls.CodeMetadataControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel Orientation="Vertical" HorizontalAlignment="Stretch">
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="{Binding MemberType}"></TextBlock>
            <TextBlock Text="{Binding ReturnType}"
                       Margin="5,0,0,0"
                       Foreground="#7AA4FF"></TextBlock>
        </StackPanel>

        <Separator Margin="0,2,0,2" />
        <TextBlock Text="{Binding Description}"
                   Margin="0,0,0,0"
                   TextWrapping="Wrap"></TextBlock>

        <Separator Margin="0,2,0,2" />
        <TextBlock Text="{Binding AutoCompleteHint}"
                   TextAlignment="Left"
                   Margin="0,0,0,0"
                   TextWrapping="Wrap"></TextBlock>

    </StackPanel>
</UserControl>
