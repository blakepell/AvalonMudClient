namespace Avalon.Common.Interfaces
{
    /// <summary>
    /// An interface that represents XAML menu (resource file and class) that can be added to
    /// the main menu bar in the mud client.
    /// </summary>
    public interface IPluginMenu
    {
    }
}
