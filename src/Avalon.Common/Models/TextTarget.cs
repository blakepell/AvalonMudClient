namespace Avalon.Common.Models
{
    /// <summary>
    /// The UI element that text should be set on.
    /// </summary>
    public enum TextTarget
    {
        None,
        /// <summary>
        /// The status bar at the bottom of the screen just below the input command.
        /// </summary>
        StatusBarText
    }
}
